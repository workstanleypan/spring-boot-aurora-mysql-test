# RDS MySQL 配置模板
# 复制此文件为 .env.rds 并填入实际值
# cp .env.rds.template .env.rds

# RDS 实例端点
# 格式: database-1.xxxxx.region.rds.amazonaws.com
export RDS_ENDPOINT="database-1.xxxxx.us-east-1.rds.amazonaws.com"

# 数据库名称
export RDS_DATABASE="testdb"

# 数据库用户名
export RDS_USERNAME="admin"

# 数据库密码（敏感信息，不要提交到 Git）
export RDS_PASSWORD="your-password-here"

# Wrapper 日志级别
# 选项: SEVERE, WARNING, INFO, CONFIG, FINE, FINER, FINEST
# 生产环境推荐: INFO
# 开发环境推荐: FINE
export WRAPPER_LOG_LEVEL="INFO"

# 使用方法:
# 1. 复制此文件: cp .env.rds.template .env.rds
# 2. 编辑 .env.rds 文件，填入实际的 RDS 配置
# 3. 加载环境变量: source .env.rds
# 4. 启动应用:
#    - 生产环境（AWS Wrapper）: ./run-rds.sh prod
#    - 开发环境（AWS Wrapper）: ./run-rds.sh dev
#    - 标准驱动（无 Wrapper）: ./run-rds.sh standard
