# Aurora MySQL 配置模板
# 复制此文件为 .env 并填入实际值
# cp .env.template .env

# Aurora 集群端点（必须使用 cluster endpoint，不能使用 instance endpoint）
# 格式: database-1.cluster-xxxxx.region.rds.amazonaws.com
export AURORA_CLUSTER_ENDPOINT="database-1.cluster-xxxxx.us-east-1.rds.amazonaws.com"

# 数据库名称
export AURORA_DATABASE="testdb"

# 数据库用户名
export AURORA_USERNAME="admin"

# 数据库密码（敏感信息，不要提交到 Git）
export AURORA_PASSWORD="your-password-here"

# Wrapper 日志级别
# 选项: SEVERE, WARNING, INFO, CONFIG, FINE, FINER, FINEST
# 生产环境推荐: INFO
# 开发环境推荐: FINE
export WRAPPER_LOG_LEVEL="INFO"

# 使用方法:
# 1. 复制此文件: cp .env.template .env
# 2. 编辑 .env 文件，填入实际的 Aurora 配置
# 3. 加载环境变量: source .env
# 4. 启动应用:
#    - 生产环境: ./run-aurora.sh prod
#    - 开发环境: ./run-aurora.sh dev
