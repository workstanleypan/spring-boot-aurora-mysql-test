<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="LOG_PATH">logs</Property>
        <!-- Startup timestamp, generates new file on each startup -->
        <Property name="STARTUP_TIME">$${date:yyyyMMdd-HHmmss}</Property>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="LOG_PATTERN_CONSOLE">%d{HH:mm:ss.SSS} [%t] %highlight{%-5level} %style{%logger{36}}{cyan} - %msg%n</Property>
    </Properties>

    <Appenders>
        <!-- Console Appender -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN_CONSOLE}" disableAnsi="false"/>
        </Console>

        <!-- File - All logs (new file per startup) -->
        <File name="AppFile" fileName="${LOG_PATH}/spring-boot-${STARTUP_TIME}.log" append="false">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </File>

        <!-- File - AWS JDBC Wrapper logs (new file per startup) -->
        <File name="WrapperFile" fileName="${LOG_PATH}/wrapper-${STARTUP_TIME}.log" append="false">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </File>

        <!-- File - Error logs (new file per startup) -->
        <File name="ErrorFile" fileName="${LOG_PATH}/error-${STARTUP_TIME}.log" append="false">
            <PatternLayout pattern="${LOG_PATTERN}"/>
            <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
        </File>
    </Appenders>

    <Loggers>
        <!-- AWS JDBC Wrapper - verbose logging -->
        <!-- JUL FINEST = TRACE, FINE = DEBUG, INFO = INFO -->
        <!-- Production: recommend DEBUG (FINE), Testing: recommend TRACE (FINEST) -->
        <Logger name="software.amazon.jdbc" level="TRACE" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="WrapperFile"/>
            <AppenderRef ref="AppFile"/>
        </Logger>

        <!-- HikariCP -->
        <Logger name="com.zaxxer.hikari" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AppFile"/>
        </Logger>

        <!-- Application -->
        <Logger name="com.test" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AppFile"/>
        </Logger>

        <!-- Spring Framework -->
        <Logger name="org.springframework" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AppFile"/>
        </Logger>

        <!-- Root Logger -->
        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="AppFile"/>
            <AppenderRef ref="ErrorFile"/>
        </Root>
    </Loggers>
</Configuration>
