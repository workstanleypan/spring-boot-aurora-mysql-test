spring:
  application:
    name: spring-boot-aurora-mysql-test
  
  # Default datasource configuration - Aurora MySQL with HikariCP
  # clusterId and bgdId explanation:
  # - clusterId: Cluster topology cache identifier, connections with same clusterId share topology cache
  # - bgdId: Blue/Green deployment state identifier, used to distinguish different BG deployments
  # - Single cluster: Both can use the same value or defaults
  # - Multi-cluster: Each cluster must use different clusterId and bgdId to avoid cache/state conflicts
  datasource:
    url: jdbc:aws-wrapper:mysql://${AURORA_CLUSTER_ENDPOINT}:3306/${AURORA_DATABASE:testdb}?wrapperPlugins=initialConnection,auroraConnectionTracker,failover2,efm2,bg&wrapperLoggerLevel=${WRAPPER_LOG_LEVEL:INFO}&clusterId=${CLUSTER_ID:cluster-a}&bgdId=${BGD_ID:cluster-a}&bgHighMs=${BG_HIGH_MS:100}&bgIncreasedMs=${BG_INCREASED_MS:1000}&bgBaselineMs=${BG_BASELINE_MS:60000}&bgConnectTimeoutMs=${BG_CONNECT_TIMEOUT_MS:30000}&bgSwitchoverTimeoutMs=${BG_SWITCHOVER_TIMEOUT_MS:180000}
    username: ${AURORA_USERNAME:admin}
    password: ${AURORA_PASSWORD}
    driver-class-name: software.amazon.jdbc.Driver
    
    # HikariCP connection pool configuration
    hikari:
      pool-name: AuroraHikariPool
      minimum-idle: 20
      maximum-pool-size: 120
      idle-timeout: 300000
      max-lifetime: 600000
      connection-timeout: 10000
      validation-timeout: 5000
      connection-test-query: SELECT 1
      # Connection leak detection - disabled (0) because persistent connection tests hold connections for long time
      # To enable, set to milliseconds value like 60000 (60 seconds)
      leak-detection-threshold: 0

# Logging configuration (Log4j2 config in log4j2-spring.xml)
logging:
  config: classpath:log4j2-spring.xml

# Actuator configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

# Server configuration
server:
  port: 8080

---
# Aurora MySQL production environment
spring:
  config:
    activate:
      on-profile: aurora-prod
  datasource:
    url: jdbc:aws-wrapper:mysql://${AURORA_CLUSTER_ENDPOINT}:3306/${AURORA_DATABASE:testdb}?wrapperLoggerLevel=${WRAPPER_LOG_LEVEL:INFO}&wrapperPlugins=initialConnection,auroraConnectionTracker,failover2,efm2,bg&clusterId=${CLUSTER_ID:cluster-a}&bgdId=${BGD_ID:cluster-a}&bgHighMs=${BG_HIGH_MS:100}&bgIncreasedMs=${BG_INCREASED_MS:1000}&bgBaselineMs=${BG_BASELINE_MS:60000}&bgConnectTimeoutMs=${BG_CONNECT_TIMEOUT_MS:30000}&bgSwitchoverTimeoutMs=${BG_SWITCHOVER_TIMEOUT_MS:180000}${JDBC_PARAMS:}

---
# Aurora MySQL development environment (verbose logging)
spring:
  config:
    activate:
      on-profile: aurora-dev
  datasource:
    url: jdbc:aws-wrapper:mysql://${AURORA_CLUSTER_ENDPOINT}:3306/${AURORA_DATABASE:testdb}?wrapperLoggerLevel=${WRAPPER_LOG_LEVEL:FINE}&wrapperPlugins=initialConnection,auroraConnectionTracker,failover2,efm2,bg&clusterId=${CLUSTER_ID:cluster-a}&bgdId=${BGD_ID:cluster-a}&bgHighMs=${BG_HIGH_MS:100}&bgIncreasedMs=${BG_INCREASED_MS:1000}&bgBaselineMs=${BG_BASELINE_MS:60000}&bgConnectTimeoutMs=${BG_CONNECT_TIMEOUT_MS:30000}&bgSwitchoverTimeoutMs=${BG_SWITCHOVER_TIMEOUT_MS:180000}${JDBC_PARAMS:}
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20

---
# RDS MySQL production environment
spring:
  config:
    activate:
      on-profile: rds-prod
  datasource:
    url: jdbc:aws-wrapper:mysql://${RDS_ENDPOINT}:3306/${RDS_DATABASE:testdb}?wrapperPlugins=auroraConnectionTracker,failover2,efm2,bg&wrapperLoggerLevel=${WRAPPER_LOG_LEVEL:INFO}&clusterId=${CLUSTER_ID:rds-cluster}&bgdId=${BGD_ID:rds-cluster}&bgHighMs=${BG_HIGH_MS:100}&bgIncreasedMs=${BG_INCREASED_MS:1000}&bgBaselineMs=${BG_BASELINE_MS:60000}&bgConnectTimeoutMs=${BG_CONNECT_TIMEOUT_MS:30000}&bgSwitchoverTimeoutMs=${BG_SWITCHOVER_TIMEOUT_MS:180000}${JDBC_PARAMS:}
    username: ${RDS_USERNAME:admin}
    password: ${RDS_PASSWORD}

---
# RDS MySQL development environment
spring:
  config:
    activate:
      on-profile: rds-dev
  datasource:
    url: jdbc:aws-wrapper:mysql://${RDS_ENDPOINT}:3306/${RDS_DATABASE:testdb}?wrapperPlugins=auroraConnectionTracker,failover2,efm2,bg&wrapperLoggerLevel=${WRAPPER_LOG_LEVEL:FINE}&clusterId=${CLUSTER_ID:rds-cluster}&bgdId=${BGD_ID:rds-cluster}&bgHighMs=${BG_HIGH_MS:100}&bgIncreasedMs=${BG_INCREASED_MS:1000}&bgBaselineMs=${BG_BASELINE_MS:60000}&bgConnectTimeoutMs=${BG_CONNECT_TIMEOUT_MS:30000}&bgSwitchoverTimeoutMs=${BG_SWITCHOVER_TIMEOUT_MS:180000}${JDBC_PARAMS:}
    username: ${RDS_USERNAME:admin}
    password: ${RDS_PASSWORD}
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
